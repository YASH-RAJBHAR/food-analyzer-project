import numpy as np

# Simple placeholder model for Render deployment.
# Replace this stub later with a real AI predictor.
def predict_food(img_path):
    dummy = np.array([1, 2, 3])
    return "food-item" if np.sum(dummy) else "unknown"